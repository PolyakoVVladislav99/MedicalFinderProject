<Page x:Class="MedicalFinderProject.Views.FavoritesPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Избранные услуги">

    <Grid>
        <Button Content="← Назад" 
        HorizontalAlignment="Left" 
        VerticalAlignment="Top" 
        Margin="20" 
        Style="{StaticResource PrimaryButton}" 
        Width="100" 
        Click="BackButton_Click"/>
        <ListView x:Name="FavoritesList" Width="900" Margin="40,70,0,60" HorizontalAlignment="Left">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Название" Width="250">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Name}" FontSize="16"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>

                    <GridViewColumn Header="Описание" Width="350">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Description}" FontSize="14" Foreground="Gray" TextWrapping="Wrap"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>

                    <GridViewColumn Header="Цена" Width="100">
                        <GridViewColumn.CellTemplate>
                            
                            <DataTemplate>
                                <TextBlock Text="{Binding Price, StringFormat={}{0} ₽}" FontSize="16" HorizontalAlignment="Right"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</Page>
