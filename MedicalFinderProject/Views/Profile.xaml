<Page x:Class="MedicalFinderProject.Views.Profile"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:MedicalFinderProject.Views"
      mc:Ignorable="d"
      Title="Profile" Loaded="Window_Loaded" Height="1000" Width="1200">

    <Grid>
        <Button Content="← Назад" 
            HorizontalAlignment="Left" 
            VerticalAlignment="Top" 
            Margin="20" 
            Style="{StaticResource PrimaryButton}" 
            Width="100" 
            Click="BackButton_Click"/>

        <Border CornerRadius="20" Background="White" Padding="30" Width="769" 
        HorizontalAlignment="Center" VerticalAlignment="Center" 
        Effect="{DynamicResource DropShadow}" Height="629" SnapsToDevicePixels="True">

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" VerticalAlignment="Center" HorizontalAlignment="Center" Margin="0,0,0,15">
                    <TextBlock Text="Профиль" FontSize="24" FontWeight="Bold" 
                           Foreground="#2C3E50" HorizontalAlignment="Center" Margin="0,0,0,10"/>

                    <TextBlock x:Name="NameTextBlock" Text="" FontSize="20" FontWeight="Bold" 
                           Foreground="#2C3E50" HorizontalAlignment="Center" TextAlignment="Center" Margin="0,0,0,10" Width="588"/>

                    <Button Content="Загрузить фото" Style="{StaticResource PrimaryButton}" Width="147" Click="UploadPhotoButton_Click"/>
                    <Button Content="Мои назначения" Style="{StaticResource PrimaryButton}" HorizontalAlignment="Center" 
                        VerticalAlignment="Top" Width="147" Click="Button_Click_1" Margin="0,10"/>
                    <Button Content="Расписание врачей" Style="{StaticResource PrimaryButton}" Width="147" Click="ViewDoctorScheduleButton_Click"/>
                    <Button Content="Сменить пароль" Style="{StaticResource PrimaryButton}" HorizontalAlignment="Center" 
                        VerticalAlignment="Top" Width="147" Click="ForgotPassButton_Click" Margin="0,10"/>
                    <Button Content="Мед карта" Style="{StaticResource PrimaryButton}" Width="147" Click="MedicalCardButton_Click"/>

                    <TextBlock Text="Уведомления" FontSize="16" FontWeight="SemiBold"
                           Foreground="#2C3E50" HorizontalAlignment="Center" Margin="0,30,0,10"/>

                    <ListView x:Name="NotificationsListView" Height="150" Margin="0,0,0,10" HorizontalAlignment="Center" Width="588">
                        <ListView.View>
                            <GridView>
                                <GridViewColumn Header="Сообщение" DisplayMemberBinding="{Binding Message}" Width="300"/>
                                <GridViewColumn Header="Дата" DisplayMemberBinding="{Binding SentAt, StringFormat=d}" Width="150"/>
                                <GridViewColumn Header="Статус" DisplayMemberBinding="{Binding IsRead}" Width="100"/>
                            </GridView>
                        </ListView.View>
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Message}" FontSize="14" Margin="5">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsRead}" Value="True">
                                                    <Setter Property="FontWeight" Value="Normal"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding IsRead}" Value="False">
                                                    <Setter Property="FontWeight" Value="Bold"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </StackPanel>

                <Image x:Name="ProfileImage" Width="100" Height="100" HorizontalAlignment="Right" VerticalAlignment="Top" 
                   Margin="10" Grid.Column="1" />
            </Grid>
        </Border>
    </Grid>
</Page>
